<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>com.example</groupId>
  <artifactId>conan-local-check</artifactId>
  <version>0.2.9-SNAPSHOT</version>
  <packaging>jar</packaging>

  <properties>
    <maven.compiler.release>21</maven.compiler.release>
    <spring.boot.version>3.3.5</spring.boot.version>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
  </properties>

  <dependencies>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-autoconfigure</artifactId>
      <version>${spring.boot.version}</version>
    </dependency>
  </dependencies>

  <build>
    <resources>
      <resource>
        <directory>src/main/resources</directory>
      </resource>
      <resource>
        <directory>${project.basedir}/target/generated-classes/cloudlogger-swig</directory>
        <includes>
          <include>**/*.class</include>
        </includes>
      </resource>
      <resource>
        <directory>${project.basedir}/target/generated-classes/cloudlogger-impl</directory>
        <includes>
          <include>**/*.class</include>
        </includes>
      </resource>
      <resource>
        <directory>${project.basedir}/target/native</directory>
        <targetPath>native</targetPath>
        <includes>
          <include>lib/**</include>
        </includes>
      </resource>
    </resources>
  </build>
</project>
